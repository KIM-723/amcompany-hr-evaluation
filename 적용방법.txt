인사진단 employees 관계 모호성 Hotfix

오류:
Could not embed because more than one relationship was found for
'personnel_diagnoses' and 'employees'

원인:
personnel_diagnoses 테이블이 employees를 여러 FK로 참조함:
- employee_id
- department_head_id
- headquarters_head_id
- source_uploaded_by
- created_by
- updated_by

그래서 Supabase/PostgREST가 단순 employees(...) 관계를 자동 선택할 수 없음.

수정:
평가대상 직원 관계를 아래 FK로 명시:
employees!personnel_diagnoses_employee_id_fkey

적용:
1. GitHub에 아래 3개 파일 교체
   - app/diagnoses/page.tsx
   - app/diagnoses/[id]/page.tsx
   - app/diagnoses/results/page.tsx
2. Commit:
   fix: specify diagnosis subject employee relationship
3. Supabase SQL 작업 없음
4. Vercel Ready
5. 인사진단 -> 진단 결과 다시 확인
